# TODO: Take input - auth code, and tenant ID
# If a ticket exists, return a json object with ticket object and all responses
# If a ticket does not exist, prompt the user to create a ticket
# Also take & sanitize input to create a ticket (i.e. subject, content, attachment(s), category, priority, tenant)
# Also take & sanitize input to create a response (i.e. content, attachment(s), ticket to be linked to)